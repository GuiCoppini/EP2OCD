public class Operation {
  static execute(String line) {
    if(line.substring(0, 2).equals("mov")) { // é um mov
      if (line.charAt(4) == '[') { // primeira parte é da memoria
        //memory.get(dentroDos[])
      }

      if (line.charAt(line.length-1) == ']') { // segunda parte é da memoria
        //memory.get(dentroDos[])
        //coloca la dentro a parte que vem antes da virgula
       }
    }
  }
}